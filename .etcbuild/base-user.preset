# Enabled globally by default by its packages
enable wireplumber.service
enable pipewire.socket
enable pipewire-pulse.socket
enable p11-kit-server.socket
enable xdg-user-dirs-update.service

# Enabled by me
enable ssh-agent.service
enable podman-auto-update.timer
